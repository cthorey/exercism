#if !defined(HAMMING_H)
#define HAMMING_H

#include <stdexcept>
#include <string>
using namespace std;

namespace hamming {
  int compute(string seq0, string seq1);
}  // namespace hamming

#endif // HAMMING_H
